<div class="main-content">
  {{#if results}}
  {{#each results}}
  <a href="/product/{{this.id}}" target="_self" class="item-link">
    <div class="item">
      <h2 class="title">{{this.title}}</h2>
      <img class="image" src="https://storage.googleapis.com/artisan-aura-photo-bucket/{{this.thumbnail}}" width="200"
        height="150" alt="product image" />
      {{!-- <p class="description">{{this.description}}</p> --}}
      <h2 class="price">${{this.price}}</h2>
    </div>
  </a>
  {{/each}}
  {{/if}}
</div>